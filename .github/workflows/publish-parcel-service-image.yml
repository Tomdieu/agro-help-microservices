name: Publish Parcel Service Docker Image
on: [push]
jobs:
  publish-hello-docker-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build the fastapi-backend Docker image
        run: |
          cd parcel_service && docker build . --tag ghcr.io/tomdieu/agro-help-parcel-service:latest
          docker run ghcr.io/tomdieu/agro-help-parcel-service:latest
          docker push ghcr.io/tomdieu/agro-help-parcel-service:latest
